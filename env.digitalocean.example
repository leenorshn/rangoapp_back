# Variables d'environnement pour DigitalOcean
# Copiez ce fichier vers .env sur votre Droplet ou configurez dans App Platform

# ============ OBLIGATOIRES ============
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/rangodb?retryWrites=true&w=majority
MONGO_DB_NAME=rangodb
JWT_SECRET=your-very-long-and-secure-secret-key-at-least-32-characters-long

# ============ SERVEUR ============
PORT=8080

# ============ BASE DE DONNÉES (Optionnel) ============
DB_TIMEOUT_SECONDS=5
DB_CONNECT_TIMEOUT_SECONDS=10
DB_MAX_RETRIES=3

# ============ HEALTH CHECK (Optionnel) ============
HEALTH_CHECK_INTERVAL_SECONDS=30

# ============ LOGGING (Optionnel) ============
LOG_LEVEL=INFO

# ============ CORS (Important pour le frontend) ============
ALLOWED_ORIGINS=https://rangoweb-ioelziq27-leenorshns-projects.vercel.app

# ============ EXCHANGE RATES (Optionnel) ============
# EXCHANGE_RATE_USD_TO_CDF=2200.0
# EXCHANGE_RATE_USD_TO_EUR=0.92
# EXCHANGE_RATE_EUR_TO_USD=1.09
# EXCHANGE_RATE_EUR_TO_CDF=2400.0
